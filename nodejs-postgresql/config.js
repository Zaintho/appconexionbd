const config = {
  db: {
    /* do not put password or any sensitive info here, done only for demo */
    host: "localhost",
    port: "5432",
    user: "postgres",
    password: "901009",
    database: "Municipios",
  },
};

module.exports = config;
